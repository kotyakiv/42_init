# Identifiy the default gateway in the routing table

# 1st command 

netstat -rn | grep default | head -1

# 2nd command
# route get default | grep "gateway"
# route (routing tables) get (display the route)

